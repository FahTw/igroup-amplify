version: 1
frontend:
    phases:
        prebuild:
            commands:
                - 'npm install'
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: /.next
        files:
            - '**/*'
    cache:
        paths: []
